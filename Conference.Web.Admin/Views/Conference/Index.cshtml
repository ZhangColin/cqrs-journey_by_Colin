@model Conference.ConferenceInfo
@{
    ViewBag.Title = "我的会议";
    ViewBag.PageType = "page_create-conf";
}

@section Navigation {
    <div class="quick__login">@Model.OwnerName</div>
}

@section PageNavigation {
    @Html.Partial("Navigation")
}

<h1 class="page__head page__wrapper page_create-conf">
    会议详情
</h1>
<div class="page__content">
    <div class="page__wrapper content-main_create-conf">
        <div class="content-main__wrapper">
            <div class="content-main__main">
                <div class="content-main__header">发起人信息</div>
                <fieldset class="content">
                    <div class="form__fieldset">
                        <label>姓名：</label>
                        <div class="form__field">
                            <pre>@Model.OwnerName</pre>
                        </div>
                    </div>
                    <div class="form__fieldset">
                        <label>邮箱：</label>
                        <div class="form__field">
                            <pre>@Model.OwnerEmail</pre>
                        </div>
                    </div>
                    <div class="form__fieldset">
                        <label>访问码：</label>
                        <div class="form__field">
                            <pre>@Model.AccessCode</pre>
                        </div>
                    </div>
                    <div class="g-clear"></div>
                </fieldset>
                <div class="content-main__header">会议信息</div>
                <fieldset class="content">
                    <div class="form">
                        <div class="form__fieldset">
                            <label>标题：</label>
                            <div class="form__field">
                                <pre>@Model.Name</pre>
                            </div>
                        </div>
                        <div class="form__fieldset">
                            <label>标签：</label>
                            <div class="form__field">
                                <pre>@Model.Tagline</pre>
                            </div>
                        </div>
                        <div class="form__fieldset">
                            <label>会议编码(Slug)：</label>
                            <div class="form__field">
                                <pre>@Model.Slug</pre>
                            </div>
                        </div>
                        <div class="form__fieldset">
                            <label>地址：</label>
                            <div class="form__field">
                                <pre>@Model.Location</pre>
                            </div>
                        </div>
                        <div class="form__fieldset">
                            <label>推特关注：</label>
                            <div class="form__field">
                                <pre>@Model.TwitterSearch</pre>
                            </div>
                        </div>
                        <div class="form__fieldset form__fieldset_textarea">
                            <label>描述：</label>
                            <div class="form__field">
                                <pre>@Model.Description</pre>
                            </div>
                        </div>
                        <div class="form__fieldset">
                            <label>开始日期：</label>
                            <div class="form__field">
                                <pre>@Model.StartDate.ToString("yyyy-MM-dd")</pre>
                            </div>
                        </div>
                        <div class="form__fieldset">
                            <label>结束日期：</label>
                            <div class="form__field">
                                <pre>@Model.EndDate.ToString("yyyy-MM-dd")</pre>
                            </div>
                        </div>
                        <div class="g-clear"></div>
                    </div>
                </fieldset>
                <div class="nav content-main__nav">
                    <a class="nav__left" href="@Url.Action("Edit", new {slug=Model.Slug})">编辑</a>
                    @{
                        var action = Model.IsPublished ? "Unpublish" : "Publish";
                        var btnTitle = Model.IsPublished ? "取消发布" : "发布";
                    }
                    @using (Html.BeginForm(action, "Conference", new { Model.Id })) {
                        <button type="submit" class="nav__right">@btnTitle</button>
                    }
                    <a class="nav__right" style="margin-right: 40px;" href="@Url.Action("Seats", new { slug = ViewBag.Slug })">设置座位</a>
                </div>
            </div>
        </div>
        <div class="g-clear"></div>
    </div>
</div>